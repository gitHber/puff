{{!< default}}
<header
  style="background-image: url({{#if primary_author.cover_image}}{{primary_author.cover_image }}{{else}}{{@blog.cover_image}}{{/if}})"
  class="tag-header">
  {{> navigation}}
  <div class="tag-header-container">
    <div class="tag-content">
      {{#if primary_author.profile_image}}
      <img class="author-profile-image" src="{{primary_author.profile_image}}" alt="{{primary_author.name}}" />
      {{else}}
      <img class="author-profile-image" src="{{asset 'images/default_head.png'}}" alt="{{primary_author.name}}" />
      {{/if}}
      <h1 class="tag-title">{{primary_author.name}}</h1>
      {{#if primary_author.bio}}
      <h2 class="tag-description">
        {{primary_author.bio}}
      </h2>
      {{/if}}
    </div>
    <div class="tag-post-num">
      <i class="fa fa-signal"></i><span>共 {{pagination.total}} 篇文章</span>
    </div>
  </div>
</header>
<div class="site-content">
  {{> loop}}
</div>